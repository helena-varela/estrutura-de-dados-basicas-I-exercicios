#define __CHECK_INTEGRIDADE__ 1
#include <stdexcept>

/**
 * @brief Encontra o primeiro índice de um elemento no array.
 *​
 * @param arr O array ordenado de inteiros.
 * @param tamanho O tamanho do array.
 * @param alvo O elemento a ser buscado.
 * @return Índice que indica a primeiro primeira ocorrência do elemento buscado, ou -1 se não estiver presente.
 */
int encontrarPrimeiroIndice(const int arr[], int tamanho, int alvo) 
{
    throw new std::runtime_error("Ainda não implementado.");
}
